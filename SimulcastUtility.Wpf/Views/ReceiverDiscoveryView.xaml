<UserControl x:Class="SimulcastUtility.Wpf.Views.ReceiverDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             >
    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid Margin="28,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Margin="0,0,0,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Width="40" Height="40" Margin="0,0,14,0" Command="{Binding BackCommand}" Style="{DynamicResource IconButtonStyle}" ToolTip="Back to manage receivers">
                    <iconPacks:PackIconMaterial Width="18" Height="18" Kind="ArrowLeft" />
                </Button>
                <StackPanel Grid.Column="1">
                    <TextBlock Style="{DynamicResource PageTitleTextStyle}" Text="Discover Receivers" />
                    <TextBlock Margin="0,5,0,0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Text="Scan an IPv4 range and add the receivers that respond." />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="380" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Padding="22" VerticalAlignment="Top" Style="{DynamicResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Style="{DynamicResource SectionTitleTextStyle}" Text="Discovery Range" />
                        <TextBlock Margin="0,6,0,22" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Text="Both addresses must share their first three octets." TextWrapping="Wrap" />
                        <TextBlock Style="{DynamicResource LabelTextStyle}" Text="STARTING IPV4 ADDRESS" />
                        <TextBox Margin="0,7,0,17" Style="{DynamicResource ModernTextBoxStyle}" Text="{Binding StartIpAddress, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Style="{DynamicResource LabelTextStyle}" Text="ENDING IPV4 ADDRESS" />
                        <TextBox Margin="0,7,0,17" Style="{DynamicResource ModernTextBoxStyle}" Text="{Binding EndIpAddress, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Style="{DynamicResource LabelTextStyle}" Text="TIMEOUT PER ADDRESS (MS)" />
                        <TextBox Margin="0,7,0,8" MaxLength="5" Style="{DynamicResource ModernTextBoxStyle}" Text="{Binding TimeoutMilliseconds, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,0,0,14" FontSize="11" Foreground="{DynamicResource ErrorBrush}" Text="{Binding ValidationMessage}" TextWrapping="Wrap" Visibility="{Binding ValidationVisibility}" />
                        <Button Height="40" Command="{Binding DiscoverCommand}" Style="{DynamicResource PrimaryButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Width="16" Height="16" Margin="0,0,8,0" Kind="Radar" />
                                <TextBlock Text="Discover Receivers" />
                            </StackPanel>
                        </Button>
                        <Border Margin="0,18,0,0" Padding="14" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Visibility="{Binding ProgressVisibility}">
                            <StackPanel>
                                <Grid>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Text="{Binding ProgressText}" />
                                    <Button HorizontalAlignment="Right" Command="{Binding CancelDiscoveryCommand}" Content="Cancel" Style="{DynamicResource TransparentButtonStyle}" />
                                </Grid>
                                <ProgressBar Height="6" Margin="0,12,0,0" Maximum="{Binding ProgressMaximum, Mode=OneWay}" Value="{Binding ProgressValue, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Padding="0" Style="{DynamicResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Border Padding="22,18" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                            <StackPanel>
                                <TextBlock Style="{DynamicResource SectionTitleTextStyle}" Text="Discovered Receivers" />
                                <TextBlock Margin="0,5,0,0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Text="Choose the receivers to save and give each one a display name." />
                            </StackPanel>
                        </Border>
                        <Grid Grid.Row="1">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding EmptyResultsVisibility}">
                                <iconPacks:PackIconMaterial Width="42" Height="42" HorizontalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}" Kind="AccessPointNetworkOff" />
                                <TextBlock Margin="0,12,0,0" Foreground="{DynamicResource SecondaryTextBrush}" Text="Run a discovery scan to find receivers." />
                            </StackPanel>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Visibility="{Binding ResultsVisibility}">
                                <ItemsControl Margin="12" ItemsSource="{Binding DiscoveredReceivers}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,9" Padding="14" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="170" />
                                                        <ColumnDefinition Width="170" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <CheckBox Margin="0,0,14,0" VerticalAlignment="Center" IsChecked="{Binding IsSelected}" IsEnabled="{Binding CanSelect}" Style="{DynamicResource ModernCheckBoxStyle}" />
                                                    <StackPanel Grid.Column="1">
                                                        <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding IpAddress}" />
                                                        <TextBlock Margin="0,3,0,0" FontSize="11" Foreground="{DynamicResource MutedTextBrush}" Text="Verified by Discovery" />
                                                    </StackPanel>
                                                    <Border Grid.Column="2" Margin="4,0,14,0" Padding="9,4" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{DynamicResource DiscoveryStatusBadgeStyle}">
                                                        <TextBlock FontSize="10" FontWeight="SemiBold" Foreground="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=Border}}" Text="{Binding StatusText}" />
                                                    </Border>
                                                    <TextBox Grid.Column="3" VerticalAlignment="Center" IsEnabled="{Binding CanEdit}" Style="{DynamicResource ModernTextBoxStyle}" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                        <Border Grid.Row="2" Padding="18" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
                            <Button Height="40" HorizontalAlignment="Right" Command="{Binding SaveSelectedCommand}" Style="{DynamicResource SuccessButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Width="16" Height="16" Margin="0,0,8,0" Kind="ContentSaveCheck" />
                                    <TextBlock Text="Save Selected Receivers" />
                                </StackPanel>
                            </Button>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</UserControl>

# Application screenshots

This directory is reserved for the screenshots displayed in the repository README.

Use these filenames so the prepared showcase markup can be enabled without changing its links:

- `receiver-dashboard.png`
- `receiver-manager.png`
- `plugin-manager.png`
- `virtual-remote.png`

Capture each image at the same 16:9 resolution and application scale. Use demonstration receiver names and remove private IP addresses, receiver identifiers, file paths, and other sensitive information before committing a screenshot.
